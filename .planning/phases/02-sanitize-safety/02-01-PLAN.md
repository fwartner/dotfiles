---
phase: 02-sanitize-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/fwartner/dotfiles/stow/zsh/.zsh.d/exports.zsh
  - /Users/fwartner/dotfiles/.env.example
autonomous: true
requirements: [SHELL-02, SHELL-05, SHELL-06, PUB-02]

must_haves:
  truths:
    - "exports.zsh no longer contains 'pixelandprocess' or the hardcoded Fastlane password"
    - ".env.example exists and documents every secret env var with placeholder values"
    - "exports.zsh sources ~/.env.local if present, silently no-ops otherwise"
---

<objective>
Strip secrets and Pixel & Process identity from the captured shell config, and introduce a `.env.example` + `~/.env.local` mechanism so real values live outside the repo.
</objective>

<tasks>
<task>
  Sanitize stow/zsh/.zsh.d/exports.zsh:
  - Remove the hardcoded FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD value, replace with conditional sourcing from env
  - Replace `FASTLANE_USER="florian@pixelandprocess.de"` with `FASTLANE_USER="${FASTLANE_USER:-}"` (sourced from .env.local or empty)
  - Add a block at the top that sources ~/.env.local if present: `[[ -f "$HOME/.env.local" ]] && source "$HOME/.env.local"`

  Write /Users/fwartner/dotfiles/.env.example with all secret vars documented and placeholder values.
</task>
</tasks>
